# 企业级经营报告关账治理

经营报告发布前必须完成数据源对账、指标血缘、重大差异解释、责任人签署和期间快照锁定。本模块将这些控制点汇总成可审计的发布门禁。

`POST /api/enterprise/reports/close-governance` 输出对账覆盖率、未解释差异数量、阻断项和待办动作。存在数据质量或血缘问题时返回 `BLOCKED`；仅缺签署或锁定时返回 `REVIEW`；全部通过后才能 `PUBLISH`。

模块默认不替代财务判断或管理层批准。生产使用时应关联报告版本、指标快照、审批记录、发布留痕与访问权限。
